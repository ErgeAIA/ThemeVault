# CLAUDE.md

指针文件：本项目的 AI 协作契约唯一真源是 [`AGENTS.md`](./AGENTS.md)。

- 规则、硬性约束、工作流、命令与 References 一律以 `AGENTS.md` 为准。
- 协作进度 / 决策 / 调试记录见 `docs/.ai/`；交接见 `docs/handoff/`。
- 修改任何契约内容时先改 `AGENTS.md`，并追加 `docs/.ai/agents-changelog.md`。
- 禁止在本文件复制 `AGENTS.md` 正文（避免漂移）。
