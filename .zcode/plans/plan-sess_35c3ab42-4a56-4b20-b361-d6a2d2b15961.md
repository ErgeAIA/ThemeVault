# 实施计划：ADR-0006 同步 + 卡片主题名链接 + ErgeMD 协议改 AGPL-3.0

## 任务总览
两个任务合并执行（共享 ErgeMD 协议改动与文档同步）：
1. **ADR-0006 描述同步**：SVG → SPA 范式（含 ADR-0005/0007/glossary 的 gen_preview 残留）
2. **卡片主题名加链接** + **底部项目名标签规范化**（15 家族全规范品牌名）+ **ErgeMD 协议 AGPL-3.0 全仓库同步**（去除所有「自有」标注）

---

## A. ErgeMD 协议 AGPL-3.0（脚本驱动 + 重跑校验）

**为什么重跑而非手改生成物**：ergemd 的 14 主题 README/palette + 家族 README + contract.json 都是 `gen_ergemd.py` 的生成物。按硬性规则1（禁止手改生成物），必须改脚本模板 + 重跑。源码路径 `D:\Workspace\Code\RustProject\ErgeMD\src\styles\themes` 已确认存在（14 css + core/）。

**改动 `scripts/gen_ergemd.py` 4 处模板字符串**：
- L181 家族 README 来源行：`（自有项目，Rust 桌面 Markdown 阅读器）` → `（AGPL-3.0，Rust 桌面 Markdown 阅读器）`
- L267 palette.md 来源行：`ErgeMD（自有项目）` → `ErgeMD（AGPL-3.0）`
- L312 主题 README 来源行：`ErgeMD（自有项目，Rust 桌面 Markdown 阅读器，内部未发布）` → `ErgeMD（AGPL-3.0，Rust 桌面 Markdown 阅读器）`
- L315 主题 README 协议行：`自有（内部项目，未发布）` → `AGPL-3.0`

**重跑 + 校验流程**（防止上游源码漂移）：
1. 重跑前先记录：当前 `themes/ergemd/_source/contract.json` 的 required(34)/derivedOptional(58) 数量，抽查 `dark/palette.md` 前 5 个色值行
2. 跑 `python scripts/gen_ergemd.py`
3. 重跑后对比：contract 数量不变 + palette 色值行不变（仅协议字符串变）= ✅ 上游稳定，预期结果
4. 若 contract 或色值行变了 = ⚠️ 上游 ErgeMD 主题变了 → **暂停，报告你决策**（可能上游确实演进，需用户确认是否接受新色值）

---

## B. gen_index.py 改动

1. **`LICENSE_RE`（L27）扩展支持 AGPL-3.0**：
   `r"（([^（）]*?(?:Apache-2\.0|MIT)[^（）]*?)）"` → 加 `|AGPL-3\.0`
   （否则家族 README 的 AGPL-3.0 解析不到，虽然主题级 `license_of` 不限协议名仍能从主题 README 取到，但家族级兜底会缺失）

2. **加 `FAMILY_DISPLAY_NAMES` 常量 + `familyDisplayName` 字段**（与 `FAMILY_ORDER` 并列，硬编码映射——风格一致）：

| family id | 显示名 | | family id | 显示名 |
|---|---|---|---|---|
| opensquilla | OpenSquilla | | tokyonight | Tokyo Night |
| catppuccin | Catppuccin | | one-dark-pro | One Dark Pro |
| ergemd | ErgeMD | | night-owl | Night Owl |
| aura | Aura | | synthwave | Synthwave |
| dracula | Dracula | | iceberg | Iceberg |
| nord | Nord | | kanagawa | Kanagawa |
| solarized | Solarized | | everforest | Everforest |
| | | | rose-pine | Rosé Pine |

   写入 family 对象：`"familyDisplayName": FAMILY_DISPLAY_NAMES.get(fam_dir.name, fam_dir.name)`

> **请核对上表品牌名**（尤其 tokyonight→"Tokyo Night"两词、rose-pine→"Rosé Pine"带重音、one-dark-pro→"One Dark Pro"）。若有异议请在批准时指出。

---

## C. preview.html 改动

1. **`renderCard`（L197）主题名加链接**：
   `<b>${esc(theme.displayName)}</b>` → `<b><a href="${fam.sourceRepo||''}" ${fam.sourceRepo?'target="_blank" rel="noopener noreferrer"':''}>${esc(theme.displayName)}</a></b>`
   （sourceRepo 缺失时退化为纯文本链接）

2. **底部 tags（L193）用 familyDisplayName**：
   `${esc(family)}` → `${esc(fam.familyDisplayName || family)}`

3. **`findTheme`（L277-281）补字段**（对比模式下卡片能取到 sourceRepo/familyDisplayName）：
   `return {family:f.family, theme:t}` → `return {family:f.family, theme:t, sourceRepo:f.sourceRepo, familyDisplayName:f.familyDisplayName}`

4. **`renderCompare` 表头（L346）主题名加链接 + 标签用 familyDisplayName**：
   `${esc(s.theme.displayName)}` 包成 `<a href="${s.sourceRepo}" target="_blank" rel="noopener noreferrer">`；`${esc(s.family)}` → `${esc(s.familyDisplayName || s.family)}`

5. **CSS**：`.card-head b a, .cg-head a { color: inherit; text-decoration: none; }` + `:hover { text-decoration: underline; }` + `cursor: pointer`

---

## D. 手维护文档同步「自有」→AGPL-3.0

（非生成物，直接编辑。grep 已确认全部出现点）

1. `AGENTS.md` L36 硬性规则6：去除「自有项目（ergemd）协议标注『自有』」，ergemd 改标 AGPL-3.0
2. `SOURCES.md` L13（ergemd 行协议列 + 备注）、L32（维护约定：去「自有项目标注『自有』」）
3. `INDEX.md` L23-36（14 个 ergemd 主题协议列）、L83（协议分布「自有×14」→「AGPL-3.0×14」）
4. `README.md` L94（进度行「自有项目」→「AGPL-3.0 项目」）
5. `docs/migration-guide.md` L19（角色映射矩阵 ergemd 行协议列）
6. `AI-MAP.md` L54（家族现状表协议列）、L146（注意协议）、L168（ADR 索引描述）

> 注：INDEX.md L83 协议统计口径既有疑似不准（tokyonight 3 主题似算 1），属既有问题，本次只改「自有」→「AGPL-3.0」标签，不修统计口径。

---

## E. ADR 同步（SVG→SPA + gen_preview 残留 + 协议）

1. **`docs/adr/0006-preview-cards-html.md`**（核心，任务1）：
   - 决策1：`gen_preview.py` 生成 SVG 色卡 + `preview/index.html` → `gen_index.py --write` 生成 `preview/data.js`，`preview.html` 数据驱动 SPA（瀑布流 + grid auto-fill + AA 徽标 + 品牌色 swatch + 主题对比）
   - 决策3（触发时机）：每收新主题跑 `gen_index.py --write`（data.js 更新，刷新浏览器即反映，无需重生成预览）
   - 决策4（SVG 优先）：改为「SPA 数据驱动：data.js 全量 INDEX.json 包成 `window.__TV`，preview.html 双击即开，`<script src>` 无 file:// CORS 限制」
   - 决策5（字体身份提示）：`gen_preview.py 据此渲染` → SPA `renderCard` 据 fontStyle 渲染（mono 全卡片等宽 / display-mono 标题等宽 / 右上 meta ·mono 徽标 + 字体 tag）
   - 影响：`preview/*.svg + preview/index.html` → `preview/data.js + preview.html`
2. **`docs/adr/0005-contrast-aa-guard.md`** L18：`gen_preview.py 的 SVG 色卡顶部已带` → `preview.html SPA 卡片徽标已带`
3. **`docs/adr/0007-...md`**：
   - 标题：「自有项目来源」→「AGPL-3.0 开源项目来源」
   - L8 背景：去「自有项目」表述
   - L23-24 决策5来源登记：协议「自有（内部项目，未发布）」→「AGPL-3.0」；license 字段「自有」→「AGPL-3.0」
   - L25 决策6：`gen_preview.py 增加 FAMILY_SECTION_ROLES...` → `preview.html 内嵌 FAMILY_SECTION_ROLES / BADGE_ROLES / BG_TEXT_ROLES（JS 常量，从 gen_preview 移植），按家族渲染`
4. **`docs/adr/0004-...md`** L24：协议分布统计更新（ergemd 自有→AGPL-3.0）
5. **`docs/glossary.md`** L33：`preview/（预览）`词条，`gen_preview.py 生成的 SVG 色卡 + HTML 网格预览页` → `preview/data.js（gen_index.py 生成）+ preview.html 数据驱动 SPA`

---

## F. 校验与交付

1. 跑 `python scripts/gen_index.py --write` → 要求全绿（每套 missing=0 out=0，退出码 0）
2. 核对 INDEX.json：ergemd 14 主题 license=AGPL-3.0、family 级 license=AGPL-3.0、familyDisplayName 字段存在且值正确
3. **preview.html 不用 playwright MCP 验证**（你手动双击看）：主题名可点击跳转 GitHub、底部标签显示品牌名（OpenSquilla/ErgeMD/Catppuccin…）、对比模式表头链接 + 标签同步
4. 交付报告：协议改动范围、品牌名映射表、gen_ergemd 重跑校验结果（上游是否变化）、AA 标红项（不变，硬性规则7）

---

## 不在本次范围
- 不碰外部 ErgeMD 项目源码（gen_ergemd 只读不写）
- 不改 catppuccin/opensquilla 等其他家族生成器
- 不修 INDEX.md L83 协议统计口径（既有问题）
- 不碰 docs/brand-colors.md（品牌色速查，不含协议）

## 执行顺序
A（gen_ergemd 改+重跑+校验）→ B（gen_index.py）→ C（preview.html）→ D（手维护文档）→ E（ADR）→ F（gen_index --write 校验 + 报告）
