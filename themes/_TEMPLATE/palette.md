# <source>-<theme-id> 色板表

> 统一字段：语义角色 / 颜色值 / 类型 / 备注。
> 类型取值：中性 / 强调 / 功能 / 语法 / 派生 / 结构。
> 颜色值原文照抄源码（hex / rgb(a) / color-mix），不要改写。

## 中性色（背景 / 文本 / 边框）

| 语义角色 | 颜色值 | 类型 | 备注 |
|----------|--------|------|------|
| `--bg` | | 中性 | 最底层背景 |
| `--bg-surface` | | 中性 | 表面 |
| `--bg-surface-2` | | 中性 | 凹陷/次级表面 |
| `--bg-elevated` | | 中性 | 浮起层 |
| `--bg-hover` | | 中性 | hover 态 |
| `--text` | | 中性 | 主文本 |
| `--text-muted` | | 中性 | 次文本 |
| `--text-dim` | | 中性 | 弱文本 |
| `--border` | | 中性 | 边框 |
| `--border-strong` | | 中性 | 强边框 |
| `--border-focus` | | 中性 | 焦点边框 |
| `--card` | | 中性 | 卡片底 |
| `--hairline` | | 中性 | 极淡分隔线 |

## 强调色

| 语义角色 | 颜色值 | 类型 | 备注 |
|----------|--------|------|------|
| `--accent` | | 强调 | 主行动色 |
| `--accent-hover` | | 强调 | |
| `--accent-deep` | | 强调 | |
| `--accent-secondary` | | 强调 | |
| `--accent-foreground` | | 强调 | 该色上的文字 |

## 功能状态色（6 通道）

| 语义角色 | 颜色值 | 类型 | 备注 |
|----------|--------|------|------|
| `--ok` / `--ok-fill` | | 功能 | 成功 |
| `--warn` / `--warn-fill` | | 功能 | 警告 |
| `--danger` / `--danger-fill` | | 功能 | 危险 |
| `--info` / `--info-fill` | | 功能 | 信息 |
| `--queued` / `--queued-fill` | | 功能 | 排队中（第 6 通道） |

## 语法高亮色（仅代码块场景）

| 语义角色 | 颜色值 | 类型 | 备注 |
|----------|--------|------|------|
| `--syntax-comment` | | 语法 | |
| `--syntax-keyword` | | 语法 | |
| `--syntax-string` | | 语法 | |
| `--syntax-literal` | | 语法 | |
| `--syntax-title` | | 语法 | |
| `--syntax-attr` | | 语法 | |

## 派生 / 结构色（如主题有显式覆盖则填，否则标「继承 foundation 派生」）

| 语义角色 | 颜色值 | 类型 | 备注 |
|----------|--------|------|------|
| `--shadow` / `--shadow-color` | | 派生 | |
| `--scrim` | | 派生 | |
| `--elev-1` / `--elev-2` / `--elev-3` | | 派生 | |
| `--sidebar-*` | | 派生 | 如有 sidebar |
| `--focus-ring` | | 结构 | 如绑定 accent |
